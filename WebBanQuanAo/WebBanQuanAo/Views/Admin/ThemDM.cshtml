@model WebBanQuanAo.Models.DanhMucSanPham

@{
    ViewBag.Title = "Thêm mới danh mục";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}

<h2>Thêm mới danh mục</h2>

@using (Html.BeginForm("ThemDM", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.LabelFor(model => model.TenDanhMuc)
        @Html.TextBoxFor(model => model.TenDanhMuc, new { @class = "form-control", required = "required" })
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.HinhAnh)
        @Html.TextBoxFor(model => model.HinhAnh, new { @type = "file", @class = "form-control", required = "required" })
    </div>

    <button type="submit" class="btn btn-primary">Thêm mới</button>
}
